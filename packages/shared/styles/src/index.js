const { fontSize, fontFamily } = require('./tokens/typography');

const colors = require('./tokens/colors');
const spacing = require('./tokens/spaces');
const screens = require('./tokens/screens');
const borderRadius = require('./tokens/radius');

module.exports = {
  colors,
  screens,
  spacing,
  fontSize,
  fontFamily,
  borderRadius,
};
